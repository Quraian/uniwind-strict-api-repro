import { Text, View } from 'react-native';

export function App() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-red-500">Uniwind + Strict TypeScript API</Text>
    </View>
  );
}
